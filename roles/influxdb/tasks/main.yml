---
- name: Ensure package is installed
  become: true
  package:
    name: influxdb
    state: present

- name: Ensure service is enabled and running
  service:
    name: influxdb
    enabled: true
    state: started
  tags:
    - molecule-notest
