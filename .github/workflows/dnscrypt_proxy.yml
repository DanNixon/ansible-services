---
name: "dannixon.services.dnscrypt_proxy"

on:
  push:
    paths:
      - 'roles/dnscrypt_proxy/**'
      - '.github/workflows/dnscrypt_proxy.yml'
  pull_request:
    paths:
      - 'roles/dnscrypt_proxy/**'
      - '.github/workflows/dnscrypt_proxy.yml'

jobs:
  molecule:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1
    steps:
      - name: Check out code
        uses: actions/checkout@v1

      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.8

      - uses: dannixon/ansible-testing/actions/test_role@main
        with:
          role: dnscrypt_proxy
