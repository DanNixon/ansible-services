---
name: "dannixon.services.go_neb"

on:
  push:
    paths:
      - 'roles/go_neb/**'
      - '.github/workflows/go_neb.yml'
  pull_request:
    paths:
      - 'roles/go_neb/**'
      - '.github/workflows/go_neb.yml'

jobs:
  molecule:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1
    steps:
      - name: Check out code
        uses: actions/checkout@v1

      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.8

      - uses: dannixon/ansible-testing/actions/test_role@v1
        with:
          role: go_neb
